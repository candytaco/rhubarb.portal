// Map name to skybox folder under public/models/skybox. Portal 2 skyboxes are named by the
// SvcServerInfo skyName of each map (for example sky_day01_01 for mp_coop_laser_crusher); add an
// entry once the six faces have been extracted (specs/portal2-coop-replacement.md, section 5).
export const MAP_SKYBOX_MAP: { [key: string]: string } = {}
